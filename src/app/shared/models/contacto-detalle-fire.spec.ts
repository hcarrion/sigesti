import { ContactoDetalleFire } from './contacto-detalle-fire';

describe('ContactoDetalleFire', () => {
  it('should create an instance', () => {
    expect(new ContactoDetalleFire()).toBeTruthy();
  });
});
