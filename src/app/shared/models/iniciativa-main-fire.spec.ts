import { IniciativaMainFire } from './iniciativa-main-fire';

describe('IniciativaMainFire', () => {
  it('should create an instance', () => {
    expect(new IniciativaMainFire()).toBeTruthy();
  });
});
